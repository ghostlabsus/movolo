import { JSDOM } from "jsdom";

const search = async (query) => {
    

    return;
};

const scrape = async (slug) => {
    

    return;
};

const info = {
    name: "VidzStore",
    url: "https://vidzstore.to/",
    type: "scraper"
};

export { search, scrape, info };